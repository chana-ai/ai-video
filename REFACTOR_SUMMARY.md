# Value Assets 页面重构总结

## 更改日期
2026-08-15

## 更改概述
重新设计了音频配置部分的UI，新增声音克隆功能，删除了针对 digit_human 的特殊代码，优化了UI布局。

## 主要更改

### 1. 新增组件

#### VoiceCloningTab.tsx
- **功能**: 声音克隆录制区域
- **特性**:
  - 麦克风录音功能
  - 录制文本输入
  - 录制音频预览
  - 保存克隆声音按钮
  - 15秒自动停止计时器
  - 实时计时器显示（0/15 秒）
  - 15秒时红色警告提示
  - 自动停止功能

#### VoiceSynthesisTab.tsx
- **功能**: 语音合成模式配置
- **特性**:
  - 性别选择（男/女）
  - 语音模型选择
  - 补充描述输入
  - 紧凑布局设计

### 2. 重构组件

#### AudioConfigTab.tsx
- **重构内容**:
  - 添加模式选择（二选一）
    - 语音合成模式
    - 声音克隆模式
  - **供应商和情感选择** → 移到顶部作为公共部分，两种模式共享
  - 语音合成相关配置 → 仅在 TTS 模式下显示
  - 声音克隆相关配置 → 仅在克隆模式下显示
  - **试听效果** → 仅保留音频播放器，删除重复的"生成试听音频"按钮
  - 主播音声音开关 → 仅在主播模式下显示

### 3. 删除内容

#### page.tsx 中的变更
- 删除 `isDigitHumanMode` 状态
- 删除 `DigitHumanAssets` 组件引用
- 删除数字人模式相关的 UI 元素：
  - 数字人模式 Badge
  - 数字人创作说明 Card
  - 数字人资产组件

#### page.tsx 导入
- 删除 `DigitHumanAssets` 导入
- 删除 `Badge` 导入
- 修复 API 响应访问（`response.data.voice_path`）

#### 其他清理
- 删除 `DigitHumanAssets.tsx` 文件（不再使用）

## UI 设计原则

### 1. 二选一模式
```
语音配置模式
  ├─ 语音合成
  └─ 声音克隆
```

### 2. 模式说明
- **语音合成模式**:
  - 公共部分：供应商、情感
  - 独立部分：性别、语音模型、补充描述
  - 公共部分：试听效果（仅音频播放器）

- **声音克隆模式**:
  - 独立部分：录音区域（麦克风、计时器、文本、预览、保存）
  - 公共部分：试听效果（仅音频播放器）
  - 删除重复的"生成试听音频"按钮

### 3. 紧凑设计
- 所有表单项使用更小的内边距（h-10）
- 使用 grid 布局并行显示相关选项
- 减少不必要的间距
- 统一按钮样式和尺寸

### 4. 录音功能
- 最长录制时间：15秒
- 自动停止计时器
- 实时显示录制时长
- 15秒时红色警告提示
- 计时器自动归零

## 技术实现

### 状态管理
```typescript
voiceConfig.mode = 'tts' | 'cloning'  // 模式选择
```

### 录音计时器
```typescript
// 15秒自动停止
recordingTimerRef.current = setInterval(() => {
    setRecordingTime(prev => {
        if (prev >= 15) {
            stopRecording()
            return 15
        }
        return prev + 1
    })
}, 1000)
```

### API 集成
- 语音合成: `/api/v2/voice/preview`
- 声音克隆: 使用浏览器 MediaRecorder API

### 类型定义
保持使用 `VoiceConfig` 类型，扩展 `mode` 字段。

## 文件清单

### 新增文件
1. `src/app/ai/projects/components/value-assets/VoiceCloningTab.tsx` - 声音克隆录制组件
2. `src/app/ai/projects/components/value-assets/VoiceSynthesisTab.tsx` - 语音合成配置组件

### 修改文件
1. `src/app/ai/projects/components/value-assets/AudioConfigTab.tsx` - 重构音频配置布局
2. `src/app/ai/projects/value-assets/page.tsx` - 删除 digit_human 代码

### 删除文件
1. `src/app/ai/projects/components/DigitHumanAssets.tsx` - 不再使用的数字人组件

## UI 变更详情

### 之前的布局
```
模式选择
供应商（仅TTS） ← 独立
情感（仅TTS）   ← 独立
语音合成配置   ← 独立
声音克隆配置   ← 独立
生成试听音频按钮
试听音频播放器
保存按钮
```

### 现在的布局
```
模式选择
供应商、情感 ← 公共（两种模式共享）
语音合成配置（仅TTS显示）
声音克隆配置（仅克隆模式显示）
试听音频播放器（无生成按钮）
保存按钮
```

## 测试建议

1. **语音合成模式**:
   - 测试性别选择
   - 测试语音模型选择
   - 测试供应商切换
   - 测试情感选择
   - 测试补充描述输入
   - 测试试听效果（音频播放器）
   - 测试保存配置

2. **声音克隆模式**:
   - 测试麦克风权限请求
   - 测试录制功能
   - 测试录制文本输入
   - 测试实时计时器（0/15秒）
   - 测试15秒自动停止
   - 测试15秒红色警告
   - 测试音频预览
   - 测试保存克隆声音
   - 测试试听效果（音频播放器）

3. **模式切换**:
   - 测试两种模式间的切换
   - 测试状态保存和恢复
   - 测试保存按钮在不同模式下的行为
   - 测试供应商和情感在模式切换后保持

4. **响应式布局**:
   - 测试不同屏幕尺寸
   - 测试供应商和情感选择在窄屏下的布局

## 优势

1. **更清晰的信息层级**：供应商和情感作为公共部分，让用户更容易理解两种模式的关系
2. **更简洁的UI**：删除重复的"生成试听音频"按钮，减少用户困惑
3. **更精确的录音控制**：15秒限制和实时计时器让用户清楚地知道录制时长
4. **更好的视觉反馈**：15秒时的红色警告提示和动画效果，提醒用户录音即将结束
